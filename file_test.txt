int y is 14;
int x is 32;
if (y < x) {
    while (x >= 0) {
        int x is x - 1;
    }
}
show(x);
show(y);
arg(Q);
show(Q);
bool a is true && false && x < y;
string s is "fwa";
if (s == "fwa") {
    show(s);
}

string badString is "hello";
int badInt is -1;
int badA is badInt + badInt;
show(badA);
int badRes is "dadw" + badInt;

