int y is 14;
int x is 32;
if (y < x) {
    while (x > 0) {
        int x is x - 1;
        show(x);
    }
    
}
show(x);
show(y);

bool a is true && false && x < y;
string s is "fwa";
if (s == "fwa") {
    int x is 34;
    @show(x);
    if (x < y) {
        @show(x);
    }
}

