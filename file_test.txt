int y is 14;
int x is 3;
@arg(T);
@show(T);
while (x > 0) {
    if (y > 10) {
        while (y > x) {
            int y is x - 1;
        }
    }
    int x is x - 1;
}
show(x);
show(y);

bool a is true && false && x < y;
string s is "fwa";
if (s == "fwa") {
    int x is 34;
    @show(x);
    if (x < y) {
        @show(x);
    }
}

