int y is 42;
int x is 12;
while (x > 0) {
    int x is x-1;
}
bool a is true && false && x < y;
string s is "fwa";
if (s == "fwa") {
    int x is 34;
    show(s);
    show(x);
    @show(x);
    if (x < y) {
        @show(x);
    }
}

