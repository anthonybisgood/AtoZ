int y is 42;
int x is y - 12;
while (x > 0) {
    int x is x-1;
}
show(x);
bool a is true && false && x < y;
string s is "fwa";
if (s == "fwa") {
    int x is 34;
    show(s);
    show(x);
    @show(x);
    if (x < y) {
        @show(x);
    }
}

