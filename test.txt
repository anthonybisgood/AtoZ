@fail_test.txt
string s is "hello world";
show("dij2ue2{}2-");


if (s == s) {
    show("String equality works");

int y is 14;
int x is 32;
int Q is 42;

while (x >= 0) {
    int x is x - 1;
}


show("ffff");
show(y);

show(Q);
bool a is true && false && x < y;
string s is "fwa";
if (s == "fwa") {
    show(s);

}

int y is 100;
int x is 200;
int z is 0;
while (x >= 0) {
    while (z < 100) {
        int z is z + 1;
        int y is y + 5;
    }
    int x is x - 1;
    int y is y - 1;
}

show(x);
show(y);
show(z);
