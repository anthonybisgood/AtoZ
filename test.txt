@fail_test.txt
string s is "hello world";
show("dij2ue2{}2-");


if (s == s) {
    show("String equality works");

int y is 14;
int x is 32;
int Q is 42;

while (x >= 0) {
    while (Q >= 20) {
        int Q is Q - 1;
        show(Q);
    }
    int x is x - 1;
}


show("ffff");
show(y);

show(Q);
bool a is true && false && x < y;
string s is "fwa";
if (s == "fwa") {
    show(s);
}
show(x);
show(y);

string first is "this ";
string second is "works!";
string together is first + second;

string another is "one " + "two!";

show(together);
show(another);
